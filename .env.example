# ── Supabase ──────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here   # server-only, never expose to client

# ── Anthropic (Claude) ────────────────────────────────────
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# ── App ───────────────────────────────────────────────────
# Update NEXT_PUBLIC_APP_URL when a domain is acquired (see domain-checklist in memory)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ── External APIs (Phase 2+, add when ready) ──────────────
NEXT_PUBLIC_MAPBOX_TOKEN=your_mapbox_token
# OPENWEATHERMAP_API_KEY=your_key

# ── Tier 2: Email Forwarding (future) ─────────────────────
# POSTMARK_API_KEY=your_postmark_key
# POSTMARK_INBOUND_HASH=your_inbound_hash
